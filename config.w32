// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("CModel", "for CModel support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("CModel", "enable CModel support", "no");

if (PHP_CMODEL != "no") {
	EXTENSION("CModel", "CModel.c");
}

